# LOM inventory — Live 12.4.3

Generated by runtime introspection inside Ableton Live (`tools/introspect/`), rendered by `tools/render_inventory.py`. **Do not edit by hand; regenerate.**

- Live: 12.4.3
- Python inside Live: `3.11.6`
- Dumped: 2026-08-05 20:10:02 (pass `settled`), platform `darwin`
- Raw data with full docstrings and the instance-graph evidence: `docs/lom-raw.json`

## Instance coverage

30 of 83 module classes were met as real instances in the set that was open when this was dumped; the rest exist in dir(Live) but nothing here has ever confirmed one. Per class, the header below says which.

Never met: `ATimeableValueVector`, `BeatTime`, `BrowserItemVector`, `CCFeedbackRule`, `CcControlDevice`, `ControlDescription`, `ControlDescriptionVector`, `ControlSurfaceProxy`, `CuePoint`, `DeviceContainer`, `DeviceIO`, `DriftDevice`, `DrumCellDevice`, `EnvelopeEvent`, `EnvelopeEventControlCoefficients`, `EnvelopeEventVector`, `FloatVector`, `HybridReverbDevice`, `IntU64Vector`, `IntVector`, `LimitationError`, `ListenerHandle`, `ListenerVector`, `LomObject`, `LooperDevice`, `MeldDevice`, `MidiNote`, `MidiNoteSpecification`, `MidiNoteVector`, `NoteFeedbackRule`, `ObjectVector`, `PitchBendFeedbackRule`, `PitchClassAndOctave`, `ProgressDialog`, `PythonLicensingBridge`, `ReferencePitch`, `RoarDevice`, `RoutingChannelVector`, `RoutingTypeVector`, `ShifterDevice`, `SmptTime`, `SpectralResonatorDevice`, `StartupDialogServes as an entry point for the user to authorize Live on first launch.`, `StringVector`, `Text`, `Timer`, `TuningSystem`, `UnavailableFeatureVector`, `UnlockStatus`, `Variants`, `Vector`, `WarpMarkerVector`, `WavetableDevice`

## Module index

- **Application**: `Application`, `ControlDescription`, `ControlDescriptionVector`, `ControlSurfaceProxy`, `UnavailableFeatureVector`, `Variants`, `MessageButtons`, `PushDialogType`, `UnavailableFeature`
- **Base**: `FloatVector`, `IntU64Vector`, `IntVector`, `LimitationError`, `ObjectVector`, `StringVector`, `Text`, `Timer`, `Vector`
- **Browser**: `Browser`, `BrowserItem`, `BrowserItemIterator`, `BrowserItemVector`, `FilterType`, `Relation`
- **CcControlDevice**: `CcControlDevice`
- **Chain**: `Chain`
- **ChainMixerDevice**: `ChainMixerDevice`
- **Clip**: `Clip`, `MidiNote`, `MidiNoteSpecification`, `MidiNoteVector`, `WarpMarker`, `WarpMarkerVector`, `ClipLaunchQuantization`, `GridQuantization`, `LaunchMode`, `WarpMode`
- **ClipSlot**: `ClipSlot`, `ClipSlotPlayingState`
- **CompressorDevice**: `CompressorDevice`
- **Conversions**: `AudioToMidiType`
- **Device**: `ATimeableValueVector`, `Device`, `DeviceType`
- **DeviceIO**: `DeviceIO`
- **DeviceParameter**: `DeviceParameter`, `AutomationState`, `ParameterState`
- **DriftDevice**: `DriftDevice`
- **DrumCellDevice**: `DrumCellDevice`
- **DrumChain**: `DrumChain`
- **DrumPad**: `DrumPad`
- **Envelope**: `Envelope`, `EnvelopeEvent`, `EnvelopeEventControlCoefficients`, `EnvelopeEventVector`
- **Eq8Device**: `Eq8Device`, `EditMode`, `GlobalMode`
- **Groove**: `Groove`, `Base`
- **GroovePool**: `GroovePool`
- **HybridReverbDevice**: `HybridReverbDevice`
- **Licensing**: `ProgressDialog`, `PythonLicensingBridge`, `StartupDialogServes as an entry point for the user to authorize Live on first launch.`, `UnlockStatus`, `TrialContext`
- **Listener**: `ListenerHandle`, `ListenerVector`
- **LomObject**: `LomObject`
- **LooperDevice**: `LooperDevice`
- **MaxDevice**: `MaxDevice`
- **MeldDevice**: `MeldDevice`
- **MidiMap**: `CCFeedbackRule`, `NoteFeedbackRule`, `PitchBendFeedbackRule`, `MapMode`
- **MixerDevice**: `MixerDevice`
- **PluginDevice**: `PluginDevice`
- **RackDevice**: `RackDevice`
- **RoarDevice**: `RoarDevice`
- **Sample**: `Sample`, `SlicingBeatDivision`, `SlicingStyle`, `TransientLoopMode`
- **Scene**: `Scene`
- **ShifterDevice**: `ShifterDevice`
- **SimplerDevice**: `SimplerDevice`, `PlaybackMode`, `SlicingPlaybackMode`
- **Song**: `BeatTime`, `CuePoint`, `SmptTime`, `Song`, `CaptureDestination`, `CaptureMode`, `Quantization`, `RecordingQuantization`, `SessionRecordStatus`, `TimeFormat`
- **SpectralResonatorDevice**: `SpectralResonatorDevice`
- **TakeLane**: `TakeLane`
- **Track**: `DeviceContainer`, `RoutingChannel`, `RoutingChannelVector`, `RoutingType`, `RoutingTypeVector`, `Track`, `DeviceInsertMode`, `RoutingChannelLayout`, `RoutingTypeCategory`
- **TuningSystem**: `PitchClassAndOctave`, `ReferencePitch`, `TuningSystem`
- **WavetableDevice**: `WavetableDevice`, `EffectMode`, `FilterRouting`, `ModulationSource`, `UnisonMode`, `VoiceCount`, `Voicing`

---

## Module `Live.Application`

Module functions:

- `combine_apcs() -> bool`
- `encrypt_challenge((int)dongle1, (int)dongle2 [, (int)key_index=0]) -> tuple`
- `encrypt_challenge2((int)arg1) -> int`
- `get_application() -> Application`
- `get_random_int((int)arg1, (int)arg2) -> int`

Enum `MessageButtons`: OK_BUTTON=0, OK_NEW_SET_BUTTON=1, OK_RETRY_BUTTON=2, SAVE_DONT_SAVE_BUTTON=3, OK_ACCOUNT_BUTTON=4, OK_PURCHASE_BUTTON=5

Enum `PushDialogType`: MESSAGE_BOX=0, OUT_OF_UNLOCKS_DIALOG=5, RENT_TO_OWN_LICENSE_EXPIRED_DIALOG=7

Enum `UnavailableFeature`: note_velocity_ranges_and_probabilities=0

### `Application`
*bases: LomObject — This class represents the Live application. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `average_process_usage` | R | Reports Live's average CPU load. |
| `browser` | R | Returns an interface to the browser. |
| `canonical_parent` | R | Returns the canonical parent of the application. |
| `control_surfaces` | R | Const access to a list of the control surfaces selected in preferences, in the same order. |
| `current_dialog_button_count` | R | Number of buttons on the current dialog. |
| `current_dialog_message` | R | Text of the last dialog that appeared; Empty if all dialogs just disappeared. |
| `number_of_push_apps_running` | R | Returns the number of connected Push apps. |
| `open_dialog_count` | R | The number of open dialogs in Live. 0 if not dialog is open. |
| `peak_process_usage` | R | Reports Live's peak CPU load. |
| `unavailable_features` | R | List of features that are unavailable due to limitations of the current Live edition. |
| `view` | R | Returns the applications view component. |

Methods:

- `get_bugfix_version((Application)arg1) -> int`
- `get_build_id((Application)arg1) -> str`
- `get_document((Application)arg1) -> Song`
- `get_major_version((Application)arg1) -> int`
- `get_minor_version((Application)arg1) -> int`
- `get_variant((Application)arg1) -> str`
- `get_version_string((Application)arg1) -> str`
- `has_option((Application)arg1, (object)arg2) -> bool`
- `press_current_dialog_button((Application)arg1, (int)arg2) -> None`
- `show_message((Application)arg1, (Text)text [, (int)buttons=Application.MessageButtons.OK_BUTTON [, (bool)enable_markup=False [, (bool)show_success_icon=False]]]) -> int`
- `show_on_the_fly_message((Application)arg1, (str)message [, (int)buttons=Application.MessageButtons.OK_BUTTON [, (bool)enable_markup=False [, (bool)show_success_icon=False [, (int)push_dialog_type=Application.PushDialogType.MESSAGE_BOX]]]]) -> int`

Listeners (`add_/remove_/…_has_listener`): `average_process_usage`, `control_surfaces`, `open_dialog_count`, `peak_process_usage`, `unavailable_features`

### `Application.View`
*bases: LomObject — This class represents the view aspects of the Live application. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `browse_mode` | R | Return true if HotSwap mode is active for any target. |
| `canonical_parent` | R | Get the canonical parent of the application view. |
| `focused_document_view` | R | Return the name of the document view ('Session' or 'Arranger') |

Methods:

- `available_main_views((View)arg1) -> StringVector`
- `focus_view((View)arg1, (object)arg2) -> None`
- `hide_view((View)arg1, (object)arg2) -> None`
- `is_view_visible((View)arg1, (object)identifier [, (bool)main_window_only=True]) -> bool`
- `scroll_view((View)arg1, (int)arg2, (object)arg3, (bool)arg4) -> None`
- `show_view((View)arg1, (object)arg2) -> None`
- `toggle_browse((View)arg1) -> None`
- `zoom_view((View)arg1, (int)arg2, (object)arg3, (bool)arg4) -> None`

Enum `Application.View.NavDirection`: up=0, down=1, left=2, right=3

Listeners (`add_/remove_/…_has_listener`): `browse_mode`, `focused_document_view`, `is_view_visible`, `view_focus_changed`

### `ControlDescription`
*Describes a control present in a control surface proxy — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `id` | R |  |
| `name` | R |  |

### `ControlDescriptionVector`
*A container for returning control descriptions. — never met as an instance on this machine*

Methods:

- `append((ControlDescriptionVector)arg1, (object)arg2) -> None`
- `extend((ControlDescriptionVector)arg1, (object)arg2) -> None`

### `ControlSurfaceProxy`
*Represents a control surface running in a different process. For use by M4L — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `control_descriptions` | R |  |
| `pad_layout` | R | The layout of pads on Push. |
| `type_name` | R |  |

Methods:

- `enable_receive_midi((ControlSurfaceProxy)arg1, (bool)arg2) -> None`
- `fetch_received_midi_messages((ControlSurfaceProxy)arg1) -> tuple`
- `fetch_received_values((ControlSurfaceProxy)arg1) -> tuple`
- `grab_control((ControlSurfaceProxy)arg1, (int)arg2) -> None`
- `release_control((ControlSurfaceProxy)arg1, (int)arg2) -> None`
- `send_midi((ControlSurfaceProxy)arg1, (tuple)arg2) -> None`
- `send_value((ControlSurfaceProxy)arg1, (tuple)arg2) -> None`
- `subscribe_to_control((ControlSurfaceProxy)arg1, (int)arg2) -> None`
- `unsubscribe_from_control((ControlSurfaceProxy)arg1, (int)arg2) -> None`

Listeners (`add_/remove_/…_has_listener`): `control_values_arrived`, `midi_received`, `pad_layout`

### `UnavailableFeatureVector`
*A container for returning unavailable features. — never met as an instance on this machine*

Methods:

- `append((UnavailableFeatureVector)arg1, (object)arg2) -> None`
- `extend((UnavailableFeatureVector)arg1, (object)arg2) -> None`

### `Variants`
*Holds strings representing what type of Live is running. — never met as an instance on this machine*

- other `BETA` (str) str(object='') -> str
- other `INTRO` (str) str(object='') -> str
- other `LITE` (str) str(object='') -> str
- other `STANDARD` (str) str(object='') -> str
- other `SUITE` (str) str(object='') -> str
- other `TRIAL` (str) str(object='') -> str

---

## Module `Live.Base`

Module functions:

- `get_text((str)classname, (str)textname) -> Text`
- `log((str)arg1) -> None`
- `subst_args((Text)text [, (str)arg1='' [, (str)arg2='' [, (str)arg3='' [, (str)arg4='' [, (str)arg5='']]]]]) -> str`

### `FloatVector`
*A simple container for returning floats from Live. — never met as an instance on this machine*

Methods:

- `append((FloatVector)arg1, (object)arg2) -> None`
- `extend((FloatVector)arg1, (object)arg2) -> None`

### `IntU64Vector`
*A simple container for returning unsigned long integers from Live. — never met as an instance on this machine*

Methods:

- `append((IntU64Vector)arg1, (object)arg2) -> None`
- `extend((IntU64Vector)arg1, (object)arg2) -> None`

### `IntVector`
*A simple container for returning integers from Live. — never met as an instance on this machine*

Methods:

- `append((IntVector)arg1, (object)arg2) -> None`
- `extend((IntVector)arg1, (object)arg2) -> None`

### `LimitationError`
*bases: Exception — never met as an instance on this machine*

Methods:

- `Exception.add_note(note) --`
- `Exception.with_traceback(tb) --`

- other `args` (getset_descriptor) 

### `ObjectVector`
*A simple read only container for returning python objects. — never met as an instance on this machine*

Methods:

- `append((ObjectVector)arg1, (object)arg2) -> None`
- `extend((ObjectVector)arg1, (object)arg2) -> None`

### `StringVector`
*A simple container for returning strings from Live. — never met as an instance on this machine*

Methods:

- `append((StringVector)arg1, (object)arg2) -> None`
- `extend((StringVector)arg1, (object)arg2) -> None`

### `Text`
*A translatable, immutable string. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `text` | R |  |

### `Timer`
*A timer that will trigger a callback after a certain inverval. The timer can be repeated and will trigger the callback every interval. Errors in the callback will stop the timer. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `running` | R |  |

Methods:

- `restart((Timer)arg1) -> None`
- `start((Timer)arg1) -> None`
- `stop((Timer)arg1) -> None`

### `Vector`
*A simple read only container for returning objects from Live. — never met as an instance on this machine*

Methods:

- `append((Vector)arg1, (object)arg2) -> None`
- `extend((Vector)arg1, (object)arg2) -> None`

---

## Module `Live.Browser`

Enum `FilterType`: disabled=-1, hotswap_off=0, instrument_hotswap=1, audio_effect_hotswap=2, midi_effect_hotswap=3, drum_pad_hotswap=4, midi_track_devices=5, samples=6, count=7

Enum `Relation`: ancestor=0, equal=1, descendant=2, none=3

### `Browser`
*bases: LomObject — This class represents the live browser data base. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `audio_effects` | R | Returns a browser item with access to all the Audio Effects content. |
| `clips` | R | Returns a browser item with access to all the Clips content. |
| `colors` | R | Returns a list of browser items containing the configured colors. |
| `current_project` | R | Returns a browser item with access to all the Current Project content. |
| `drums` | R | Returns a browser item with access to all the Drums content. |
| `filter_type` | RW | Bang triggered when the hotswap target has changed. |
| `hotswap_target` | RW | Bang triggered when the hotswap target has changed. |
| `instruments` | R | Returns a browser item with access to all the Instruments content. |
| `legacy_libraries` | R | Returns a list of browser items containing the installed legacy libraries. The list is always empty as legacy library handling has been removed. |
| `max_for_live` | R | Returns a browser item with access to all the Max For Live content. |
| `midi_effects` | R | Returns a browser item with access to all the Midi Effects content. |
| `packs` | R | Returns a browser item with access to all the Packs content. |
| `plugins` | R | Returns a browser item with access to all the Plugins content. |
| `samples` | R | Returns a browser item with access to all the Samples content. |
| `sounds` | R | Returns a browser item with access to all the Sounds content. |
| `user_folders` | R | Returns a list of browser items containing all the user folders. |
| `user_library` | R | Returns a browser item with access to all the User Library content. |

Methods:

- `load_item((Browser)arg1, (BrowserItem)arg2) -> None`
- `preview_item((Browser)arg1, (BrowserItem)arg2) -> None`
- `relation_to_hotswap_target((Browser)arg1, (BrowserItem)arg2) -> Relation`
- `stop_preview((Browser)arg1) -> None`

Listeners (`add_/remove_/…_has_listener`): `filter_type`, `full_refresh`, `hotswap_target`

### `BrowserItem`
*This class represents an item of the browser hierarchy. — seen live*

| property | writable | doc |
|---|---|---|
| `children` | R | Const access to the descendants of this browser item. |
| `is_device` | R | Indicates if the browser item represents a device. |
| `is_folder` | R | Indicates if the browser item represents folder. |
| `is_loadable` | R | True if item can be loaded via the Browser's 'load_item' method. |
| `is_selected` | R | True if the item is ancestor of or the actual selection. |
| `iter_children` | R | Const iterable access to the descendants of this browser item. |
| `name` | R | Const access to the canonical display name of this browser item. |
| `source` | R | Specifies where does item come from -- i.e. Live pack, user library... |
| `uri` | R | The uri describes a unique identifier for a browser item. |

### `BrowserItemIterator`
*This class iterates over children of another BrowserItem. — seen live*

### `BrowserItemVector`
*A container for returning browser items from Live. — never met as an instance on this machine*

Methods:

- `append((BrowserItemVector)arg1, (object)arg2) -> None`
- `extend((BrowserItemVector)arg1, (object)arg2) -> None`

---

## Module `Live.CcControlDevice`

### `CcControlDevice`
*bases: Device — This class represents a CcControl device. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `custom_bool_target` | RW | Return the custom bool target |
| `custom_bool_target_list` | R | Return the custom bool target list |
| `custom_float_target_0` | RW | Return the custom float target 0 |
| `custom_float_target_0_list` | R | Return the custom float target 0 list |
| `custom_float_target_1` | RW | Return the custom float target 1 |
| `custom_float_target_10` | RW | Return the custom float target 10 |
| `custom_float_target_10_list` | R | Return the custom float target 10 list |
| `custom_float_target_11` | RW | Return the custom float target 11 |
| `custom_float_target_11_list` | R | Return the custom float target 11 list |
| `custom_float_target_1_list` | R | Return the custom float target 1 list |
| `custom_float_target_2` | RW | Return the custom float target 2 |
| `custom_float_target_2_list` | R | Return the custom float target 2 list |
| `custom_float_target_3` | RW | Return the custom float target 3 |
| `custom_float_target_3_list` | R | Return the custom float target 3 list |
| `custom_float_target_4` | RW | Return the custom float target 4 |
| `custom_float_target_4_list` | R | Return the custom float target 4 list |
| `custom_float_target_5` | RW | Return the custom float target 5 |
| `custom_float_target_5_list` | R | Return the custom float target 5 list |
| `custom_float_target_6` | RW | Return the custom float target 6 |
| `custom_float_target_6_list` | R | Return the custom float target 6 list |
| `custom_float_target_7` | RW | Return the custom float target 7 |
| `custom_float_target_7_list` | R | Return the custom float target 7 list |
| `custom_float_target_8` | RW | Return the custom float target 8 |
| `custom_float_target_8_list` | R | Return the custom float target 8 list |
| `custom_float_target_9` | RW | Return the custom float target 9 |
| `custom_float_target_9_list` | R | Return the custom float target 9 list |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `resend((CcControlDevice)self) -> None`
- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `custom_bool_target`, `custom_float_target_0`, `custom_float_target_1`, `custom_float_target_10`, `custom_float_target_11`, `custom_float_target_2`, `custom_float_target_3`, `custom_float_target_4`, `custom_float_target_5`, `custom_float_target_6`, `custom_float_target_7`, `custom_float_target_8`, `custom_float_target_9`, `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `name`, `parameters`

### `CcControlDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.Chain`

### `Chain`
*bases: DeviceContainer — This class represents a group device chain in Live. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the chain. |
| `color` | RW | Access the color index of the Chain. |
| `color_index` | RW | Access the color index of the Chain. |
| `devices` | R | Return const access to all available Devices that are present in the chains |
| `has_audio_input` | R | return True, if this Chain can be feed with an Audio signal. This is |
| `has_audio_output` | R | return True, if this Chain sends out an Audio signal. This is |
| `has_midi_input` | R | return True, if this Chain can be feed with an Audio signal. This is |
| `has_midi_output` | R | return True, if this Chain sends out MIDI events. This is |
| `is_auto_colored` | RW | Get/set access to the auto color flag of the Chain. |
| `mixer_device` | R | Return access to the mixer device that holds the chain's mixer parameters: |
| `mute` | RW | Mute/unmute the chain. |
| `muted_via_solo` | R | Return const access to whether this chain is muted due to some other chain |
| `name` | RW | Read/write access to the name of the Chain, as visible in the track header. |
| `solo` | RW | Get/Set the solo status of the chain. Note that this will not disable the |

Methods:

- `delete_device((Chain)arg1, (int)arg2) -> None`
- `duplicate_device((Chain)arg1, (int)arg2) -> None`
- `insert_device((Chain)arg1, (str)DeviceName [, (int)DeviceIndex=-1]) -> LomObject`

Listeners (`add_/remove_/…_has_listener`): `color`, `color_index`, `devices`, `is_auto_colored`, `mute`, `muted_via_solo`, `name`, `solo`

---

## Module `Live.ChainMixerDevice`

### `ChainMixerDevice`
*bases: LomObject — This class represents a Chain's Mixer Device in Live, which gives you — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the mixer device. |
| `chain_activator` | R | Const access to the Chain's Activator Device Parameter. |
| `panning` | R | Const access to the Chain's Panning Device Parameter. |
| `sends` | R | Const access to the Chain's list of Send Amount Device Parameters. |
| `volume` | R | Const access to the Chain's Volume Device Parameter. |

Listeners (`add_/remove_/…_has_listener`): `sends`

---

## Module `Live.Clip`

Enum `ClipLaunchQuantization`: q_global=0, q_none=1, q_8_bars=2, q_4_bars=3, q_2_bars=4, q_bar=5, q_half=6, q_half_triplet=7, q_quarter=8, q_quarter_triplet=9, q_eighth=10, q_eighth_triplet=11, q_sixteenth=12, q_sixteenth_triplet=13, q_thirtysecond=14

Enum `GridQuantization`: no_grid=0, g_8_bars=1, g_4_bars=2, g_2_bars=3, g_bar=4, g_half=5, g_quarter=6, g_eighth=7, g_sixteenth=8, g_thirtysecond=9, count=10

Enum `LaunchMode`: trigger=0, gate=1, toggle=2, repeat=3

Enum `WarpMode`: beats=0, tones=1, texture=2, repitch=3, complex=4, rex=5, complex_pro=6, count=7

### `Clip`
*bases: LomObject — This class represents a Clip in Live. It can be either an Audio — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `automation_envelopes` | R | Const access to a list of all automation envelopes for this clip. |
| `available_warp_modes` | R | Available for AudioClips only. |
| `canonical_parent` | R | Get the canonical parent of the Clip. |
| `color` | RW | Get/set access to the color of the Clip (RGB). |
| `color_index` | RW | Get/set access to the color index of the Clip. |
| `end_marker` | RW | Get/Set the Clips end marker pos in beats/seconds (unit depends on warping). |
| `end_time` | R | Get the clip's end time. |
| `file_path` | R | Get the path of the file represented by the Audio Clip. |
| `gain` | RW | Available for AudioClips only. |
| `gain_display_string` | R | Return a string with the gain as dB value |
| `groove` | RW | Get the groove associated with this clip. |
| `has_envelopes` | R | Will notify if the clip gets his first envelope or the last envelope is removed. |
| `has_groove` | R | Returns true if a groove is associated with this clip. |
| `is_arrangement_clip` | R | return true if this Clip is an Arrangement Clip. |
| `is_audio_clip` | R | Return true if this Clip is an Audio Clip. |
| `is_midi_clip` | R | return true if this Clip is a MIDI Clip. |
| `is_overdubbing` | R | returns true if the Clip is recording overdubs |
| `is_playing` | RW | Get/Set if this Clip is currently playing. If the Clips trigger mode |
| `is_recording` | R | returns true if the Clip was triggered to record or is recording. |
| `is_session_clip` | R | return true if this Clip is a Session Clip. |
| `is_take_lane_clip` | R | return true if this Clip is a Take Lane Clip. |
| `is_triggered` | R | returns true if the Clip was triggered or is playing. |
| `launch_mode` | RW | Get/Set access to the launch mode setting of the Clip. |
| `launch_quantization` | RW | Get/Set access to the launch quantization setting of the Clip. |
| `legato` | RW | Get/Set access to the legato setting of the Clip |
| `length` | R | Get to the Clips length in beats/seconds (unit depends on warping). |
| `loop_end` | RW | Get/Set the loop end pos of this Clip in beats/seconds (unit depends on warping). |
| `loop_start` | RW | Get/Set the Clips loopstart pos in beats/seconds (unit depends on warping). |
| `looping` | RW | Get/Set the Clips 'loop is enabled' flag |
| `muted` | RW | Read/write access to the mute state of the Clip. |
| `name` | RW | Read/write access to the name of the Clip. |
| `pitch_coarse` | RW | Available for AudioClips only. |
| `pitch_fine` | RW | Available for AudioClips only. |
| `playing_position` | R | Constant access to the current playing position of the clip. |
| `position` | RW | Get/Set the loop position of this Clip in beats/seconds (unit depends on warping). |
| `ram_mode` | RW | Available for AudioClips only. |
| `sample_length` | R | Available for AudioClips only. |
| `sample_rate` | R | Available for AudioClips only. |
| `signature_denominator` | RW | Get/Set access to the global signature denominator of the Clip. |
| `signature_numerator` | RW | Get/Set access to the global signature numerator of the Clip. |
| `start_marker` | RW | Get/Set the Clips start marker pos in beats/seconds (unit depends on warping). |
| `start_time` | R | Get the clip's start time offset. For Session View clips, this is the time the clip was started. For Arrangement View clips, this is the offset within the arrangement. |
| `velocity_amount` | RW | Get/Set access to the velocity to volume amount of the Clip. |
| `view` | R | Get the view of the Clip. |
| `warp_markers` | R | Available for AudioClips only. |
| `warp_mode` | RW | Available for AudioClips only. |
| `warping` | RW | Available for AudioClips only. |
| `will_record_on_start` | R | returns true if the Clip will record on being started. |

Methods:

- `add_new_notes((Clip)arg1, (object)arg2) -> IntU64Vector`
- `add_warp_marker((Clip)self, (object)warp_marker) -> None`
- `apply_note_modifications((Clip)arg1, (MidiNoteVector)arg2) -> None`
- `automation_envelope((Clip)arg1, (DeviceParameter)arg2) -> Envelope`
- `beat_to_sample_time((Clip)self, (float)beat_time) -> float`
- `clear_all_envelopes((Clip)arg1) -> None`
- `clear_envelope((Clip)arg1, (DeviceParameter)arg2) -> None`
- `create_automation_envelope((Clip)arg1, (DeviceParameter)arg2) -> Envelope`
- `crop((Clip)arg1) -> None`
- `deselect_all_notes((Clip)arg1) -> None`
- `duplicate_loop((Clip)arg1) -> None`
- `duplicate_notes_by_id((Clip)self, (object)note_ids [, (object)destination_time=None [, (int)transposition_amount=0]]) -> IntU64Vector`
- `duplicate_region((Clip)self, (float)region_start, (float)region_length, (float)destination_time [, (int)pitch=-1 [, (int)transposition_amount=0]]) -> None`
- `fire((Clip)arg1) -> None`
- `get_all_notes_extended((Clip)arg1) -> MidiNoteVector`
- `get_notes((Clip)self, (float)from_time, (int)from_pitch, (float)time_span, (int)pitch_span) -> tuple`
- `get_notes_by_id((Clip)arg1, (object)note_ids) -> MidiNoteVector`
- `get_notes_extended((Clip)arg1, (int)from_pitch, (int)pitch_span, (float)from_time, (float)time_span) -> MidiNoteVector`
- `get_selected_notes((Clip)arg1) -> tuple`
- `get_selected_notes_extended((Clip)arg1) -> MidiNoteVector`
- `move_playing_pos((Clip)arg1, (float)arg2) -> None`
- `move_warp_marker((Clip)self, (float)marker_beat_time, (float)beat_time_distance) -> None`
- `note_number_to_name((Clip)self, (int)midi_pitch) -> str`
- `quantize((Clip)arg1, (int)arg2, (float)arg3) -> None`
- `quantize_pitch((Clip)arg1, (int)arg2, (int)arg3, (float)arg4) -> None`
- `remove_notes((Clip)arg1, (float)arg2, (int)arg3, (float)arg4, (int)arg5) -> None`
- `remove_notes_by_id((Clip)arg1, (object)arg2) -> None`
- `remove_notes_extended((Clip)arg1, (int)from_pitch, (int)pitch_span, (float)from_time, (float)time_span) -> None`
- `remove_warp_marker((Clip)self, (float)beat_time) -> None`
- `replace_selected_notes((Clip)arg1, (tuple)arg2) -> None`
- `sample_to_beat_time((Clip)self, (float)sample_time) -> float`
- `scrub((Clip)self, (float)scrub_position) -> None`
- `seconds_to_sample_time((Clip)self, (float)seconds) -> float`
- `select_all_notes((Clip)arg1) -> None`
- `select_notes_by_id((Clip)arg1, (object)arg2) -> None`
- `set_fire_button_state((Clip)arg1, (bool)arg2) -> None`
- `set_notes((Clip)arg1, (tuple)arg2) -> None`
- `stop((Clip)arg1) -> None`
- `stop_scrub((Clip)arg1) -> None`

Listeners (`add_/remove_/…_has_listener`): `color`, `color_index`, `end_marker`, `end_time`, `file_path`, `gain`, `groove`, `has_envelopes`, `is_overdubbing`, `is_recording`, `launch_mode`, `launch_quantization`, `legato`, `loop_end`, `loop_jump`, `loop_start`, `looping`, `muted`, `name`, `notes`, `pitch_coarse`, `pitch_fine`, `playing_position`, `playing_status`, `position`, `ram_mode`, `signature_denominator`, `signature_numerator`, `start_marker`, `start_time`, `velocity_amount`, `warp_markers`, `warp_mode`, `warping`

### `Clip.View`
*bases: LomObject — Representing the view aspects of a Clip. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the clip view. |
| `grid_is_triplet` | RW | Get/set wether the grid is showing in triplet mode. |
| `grid_quantization` | RW | Get/set clip grid quantization resolution. |

Methods:

- `hide_envelope((View)arg1) -> None`
- `select_envelope_parameter((View)arg1, (DeviceParameter)arg2) -> None`
- `show_envelope((View)arg1) -> None`
- `show_loop((View)arg1) -> None`

### `MidiNote`
*An object representing a MIDI Note — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `duration` | RW |  |
| `mute` | RW |  |
| `note_id` | R | A numerical ID that's unique within the originating clip of the note. Not to be |
| `pitch` | RW |  |
| `probability` | RW |  |
| `release_velocity` | RW |  |
| `start_time` | RW |  |
| `velocity` | RW |  |
| `velocity_deviation` | RW |  |

### `MidiNoteSpecification`
*An object specifying the data for creating a MIDI note. To be used with the — never met as an instance on this machine*

### `MidiNoteVector`
*A container for holding MIDI notes from Live. — never met as an instance on this machine*

Methods:

- `append((MidiNoteVector)arg1, (object)arg2) -> None`
- `extend((MidiNoteVector)arg1, (object)arg2) -> None`

### `WarpMarker`
*This class represents a WarpMarker type. — seen live*

| property | writable | doc |
|---|---|---|
| `beat_time` | R | A WarpMarker's beat time. |
| `sample_time` | R | A WarpMarker's sample time. |

### `WarpMarkerVector`
*A container for returning warp markers from Live. — never met as an instance on this machine*

Methods:

- `append((WarpMarkerVector)arg1, (object)arg2) -> None`
- `extend((WarpMarkerVector)arg1, (object)arg2) -> None`

---

## Module `Live.ClipSlot`

Enum `ClipSlotPlayingState`: stopped=0, started=1, recording=2

### `ClipSlot`
*bases: LomObject — This class represents an entry in Lives Session view matrix. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the ClipSlot. |
| `clip` | R | Returns the Clip which this clipslots currently owns. Might be None. |
| `color` | R | Returns the canonical color for the clip slot or None if it does not exist. |
| `color_index` | R | Returns the canonical color index for the clip slot or None if it does not exist. |
| `controls_other_clips` | R | Returns true if firing this slot will fire clips in other slots. |
| `has_clip` | R | Returns true if this Clipslot owns a Clip. |
| `has_stop_button` | RW | Get/Set if this Clip has a stop button, which will, if fired, stop any |
| `is_group_slot` | R | Returns whether this clip slot is a group track slot (group slot). |
| `is_playing` | R | Returns whether the clip associated with the slot is playing. |
| `is_recording` | R | Returns whether the clip associated with the slot is recording. |
| `is_triggered` | R | Const access to the triggering state of the clip slot. |
| `playing_status` | R | Const access to the playing state of the clip slot. |
| `will_record_on_start` | R | returns true if the clip slot will record on being fired. |

Methods:

- `create_audio_clip((ClipSlot)arg1, (object)arg2) -> Clip`
- `create_clip((ClipSlot)arg1, (float)arg2) -> Clip`
- `delete_clip((ClipSlot)arg1) -> None`
- `duplicate_clip_to((ClipSlot)arg1, (ClipSlot)arg2) -> None`
- `fire((ClipSlot)arg1) -> None`
- `set_fire_button_state((ClipSlot)arg1, (bool)arg2) -> None`
- `stop((ClipSlot)arg1) -> None`

Listeners (`add_/remove_/…_has_listener`): `color`, `color_index`, `controls_other_clips`, `has_clip`, `has_stop_button`, `is_triggered`, `playing_status`

---

## Module `Live.CompressorDevice`

### `CompressorDevice`
*bases: Device — This class represents a Compressor device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `available_input_routing_channels` | R | Return a list of source channels for input routing in the sidechain. |
| `available_input_routing_types` | R | Return a list of source types for input routing in the sidechain. |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `input_routing_channel` | RW | Get and set the current source channel for input routing in the sidechain. |
| `input_routing_type` | RW | Get and set the current source type for input routing in the sidechain. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `available_input_routing_channels`, `available_input_routing_types`, `input_routing_channel`, `input_routing_type`, `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `name`, `parameters`

### `CompressorDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.Conversions`

Module functions:

- `audio_to_midi_clip((Song)song, (Clip)audio_clip, (int)audio_to_midi_type) -> None`
- `create_drum_rack_from_audio_clip((Song)song, (Clip)audio_clip) -> None`
- `create_midi_track_from_drum_pad((Song)song, (DrumPad)drum_pad) -> None`
- `create_midi_track_with_simpler((Song)song, (Clip)audio_clip) -> None`
- `is_convertible_to_midi((Song)song, (Clip)audio_clip) -> bool`
- `move_devices_on_track_to_new_drum_rack_pad((Song)song, (int)track_index) -> LomObject`
- `sliced_simpler_to_drum_rack((Song)song, (SimplerDevice)simpler) -> None`

Enum `AudioToMidiType`: harmony_to_midi=0, melody_to_midi=1, drums_to_midi=2

---

## Module `Live.Device`

Enum `DeviceType`: undefined=0, instrument=1, audio_effect=2, midi_effect=4

### `ATimeableValueVector`
*never met as an instance on this machine*

Methods:

- `append((ATimeableValueVector)arg1, (object)arg2) -> None`
- `extend((ATimeableValueVector)arg1, (object)arg2) -> None`

### `Device`
*bases: LomObject — This class represents a MIDI or Audio DSP-Device in Live. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `name`, `parameters`

### `Device.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.DeviceIO`

### `DeviceIO`
*bases: LomObject — This class represents a specific input or output bus of a device. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `available_routing_channels` | R | Return a list of channels for this IO endpoint. |
| `available_routing_types` | R | Return a list of available routing types for this IO endpoint. |
| `canonical_parent` | R | Get the canonical parent of the device IO. |
| `default_external_routing_channel_is_none` | RW | Get and set whether the default routing channel for External routing types is none. |
| `routing_channel` | RW | Get and set the current routing channel. |
| `routing_type` | RW | Get and set the current routing type. |

Listeners (`add_/remove_/…_has_listener`): `available_routing_channels`, `available_routing_types`, `routing_channel`, `routing_type`

---

## Module `Live.DeviceParameter`

Enum `AutomationState`: none=0, playing=1, overridden=2

Enum `ParameterState`: enabled=0, irrelevant=1, disabled=2

### `DeviceParameter`
*bases: LomObject — This class represents a (automatable) parameter within a MIDI or — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `automation_state` | R | Returns state of type AutomationState. |
| `canonical_parent` | R | Get the canonical parent of the device parameter. |
| `default_value` | R | Return the default value for this parameter. A Default value is only |
| `display_value` | RW | Get/Set the current value (as visible in the GUI) this parameter. |
| `is_enabled` | R | Returns false if the parameter has been macro mapped or disabled by Max. |
| `is_quantized` | R | Returns True, if this value is a boolean or integer like switch. |
| `max` | R | Returns const access to the upper value of the allowed range for |
| `min` | R | Returns const access to the lower value of the allowed range for |
| `name` | R | Returns const access the name of this parameter, as visible in Lives |
| `original_name` | R | Returns const access the original name of this parameter, unaffected of |
| `short_value_items` | R | Return the list of possible values for this parameter. Like value_items, but prefers short value names if available. Raises an error if 'is_quantized' is False. |
| `state` | R | Returns the state of the parameter: |
| `value` | RW | Get/Set the current internal value of this parameter. |
| `value_items` | R | Return the list of possible values for this parameter. Raises an error if 'is_quantized' is False. |

Methods:

- `begin_gesture((DeviceParameter)arg1) -> None`
- `end_gesture((DeviceParameter)arg1) -> None`
- `re_enable_automation((DeviceParameter)arg1) -> None`
- `str_for_value((DeviceParameter)arg1, (float)arg2) -> str`

Listeners (`add_/remove_/…_has_listener`): `automation_state`, `display_value`, `name`, `state`, `value`

---

## Module `Live.DriftDevice`

### `DriftDevice`
*bases: Device — This class represents a Drift device. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `mod_matrix_filter_source_1_index` | RW | Return the filter mod source 1 index |
| `mod_matrix_filter_source_1_list` | R | Return the filter mod source 1 list |
| `mod_matrix_filter_source_2_index` | RW | Return the filter mod source 2 index |
| `mod_matrix_filter_source_2_list` | R | Return the filter mod source 2 list |
| `mod_matrix_lfo_source_index` | RW | Return the lfo mod source index |
| `mod_matrix_lfo_source_list` | R | Return the lfo mod source list |
| `mod_matrix_pitch_source_1_index` | RW | Return the pitch mod source 1 index |
| `mod_matrix_pitch_source_1_list` | R | Return the pitch mod source 1 list |
| `mod_matrix_pitch_source_2_index` | RW | Return the pitch mod source 2 index |
| `mod_matrix_pitch_source_2_list` | R | Return the pitch mod source 2 list |
| `mod_matrix_shape_source_index` | RW | Return the shape mod source index |
| `mod_matrix_shape_source_list` | R | Return the shape mod source list |
| `mod_matrix_source_1_index` | RW | Return the custom mod source 1 index |
| `mod_matrix_source_1_list` | R | Return the custom mod source 1 list |
| `mod_matrix_source_2_index` | RW | Return the custom mod source 2 index |
| `mod_matrix_source_2_list` | R | Return the custom mod source 2 list |
| `mod_matrix_source_3_index` | RW | Return the custom mod source 3 index |
| `mod_matrix_source_3_list` | R | Return the custom mod source 3 list |
| `mod_matrix_target_1_index` | RW | Return the custom mod target 1 index |
| `mod_matrix_target_1_list` | R | Return the custom mod target 1 list |
| `mod_matrix_target_2_index` | RW | Return the custom mod target 2 index |
| `mod_matrix_target_2_list` | R | Return the custom mod target 2 list |
| `mod_matrix_target_3_index` | RW | Return the custom mod target 3 index |
| `mod_matrix_target_3_list` | R | Return the custom mod target 3 list |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `pitch_bend_range` | RW | Return the Pitch Bend Range |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |
| `voice_count_index` | RW | Return the voice count index |
| `voice_count_list` | R | Return the voice count list |
| `voice_mode_index` | RW | Return the voice mode index |
| `voice_mode_list` | R | Return the voice mode list |

Methods:

- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `mod_matrix_filter_source_1_index`, `mod_matrix_filter_source_2_index`, `mod_matrix_lfo_source_index`, `mod_matrix_pitch_source_1_index`, `mod_matrix_pitch_source_2_index`, `mod_matrix_shape_source_index`, `mod_matrix_source_1_index`, `mod_matrix_source_2_index`, `mod_matrix_source_3_index`, `mod_matrix_target_1_index`, `mod_matrix_target_2_index`, `mod_matrix_target_3_index`, `name`, `parameters`, `pitch_bend_range`, `voice_count_index`, `voice_mode_index`

### `DriftDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.DrumCellDevice`

### `DrumCellDevice`
*bases: Device — This class represents a DrumCell device. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `gain` | RW | Return the Gain value |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `gain`, `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `name`, `parameters`

### `DrumCellDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.DrumChain`

### `DrumChain`
*bases: Chain — This class represents a drum group device chain in Live. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the chain. |
| `choke_group` | RW | Access to the chain's choke group setting. |
| `color` | RW | Access the color index of the Chain. |
| `color_index` | RW | Access the color index of the Chain. |
| `devices` | R | Return const access to all available Devices that are present in the chains |
| `has_audio_input` | R | return True, if this Chain can be feed with an Audio signal. This is |
| `has_audio_output` | R | return True, if this Chain sends out an Audio signal. This is |
| `has_midi_input` | R | return True, if this Chain can be feed with an Audio signal. This is |
| `has_midi_output` | R | return True, if this Chain sends out MIDI events. This is |
| `in_note` | RW | Access to the incoming MIDI note that will trigger this chain. |
| `is_auto_colored` | RW | Get/set access to the auto color flag of the Chain. |
| `mixer_device` | R | Return access to the mixer device that holds the chain's mixer parameters: |
| `mute` | RW | Mute/unmute the chain. |
| `muted_via_solo` | R | Return const access to whether this chain is muted due to some other chain |
| `name` | RW | Read/write access to the name of the Chain, as visible in the track header. |
| `out_note` | RW | Access to the MIDI note sent to the devices in the chain. |
| `solo` | RW | Get/Set the solo status of the chain. Note that this will not disable the |

Methods:

- `delete_device((Chain)arg1, (int)arg2) -> None`
- `duplicate_device((Chain)arg1, (int)arg2) -> None`
- `insert_device((Chain)arg1, (str)DeviceName [, (int)DeviceIndex=-1]) -> LomObject`

Listeners (`add_/remove_/…_has_listener`): `choke_group`, `color`, `color_index`, `devices`, `in_note`, `is_auto_colored`, `mute`, `muted_via_solo`, `name`, `out_note`, `solo`

---

## Module `Live.DrumPad`

### `DrumPad`
*bases: LomObject — This class represents a drum group device pad in Live. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the drum pad. |
| `chains` | R | Return const access to the list of chains in this drum pad. |
| `mute` | RW | Mute/unmute the pad. |
| `name` | R | Return const access to the drum pad's name. It depends on the contained chains. |
| `note` | R | Get the MIDI note of the drum pad. |
| `solo` | RW | Solo/unsolo the pad. |

Methods:

- `delete_all_chains((DrumPad)arg1) -> None`

Listeners (`add_/remove_/…_has_listener`): `chains`, `mute`, `name`, `solo`

---

## Module `Live.Envelope`

### `Envelope`
*bases: LomObject — This class represents an automation or modulation envelope in Live. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the envelope. |
| `parameter` | R | Read-only access to the parameter controlled by the envelope. |

Methods:

- `create_event((Envelope)arg1, (EnvelopeEvent)arg2) -> None`
- `delete_events_in_range((Envelope)arg1, (float)arg2, (float)arg3) -> None`
- `events_in_range((Envelope)arg1, (float)arg2, (float)arg3) -> EnvelopeEventVector`
- `insert_step((Envelope)arg1, (float)arg2, (float)arg3, (float)arg4) -> None`
- `value_at_time((Envelope)arg1, (float)arg2) -> float`

### `EnvelopeEvent`
*This is a class that represents an envelope event. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `control_coefficients` | RW |  |
| `time` | RW |  |
| `value` | RW |  |

### `EnvelopeEventControlCoefficients`
*This class represents the control coefficients of an envelope event. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `x1` | RW |  |
| `x2` | RW |  |
| `y1` | RW |  |
| `y2` | RW |  |

### `EnvelopeEventVector`
*A container for holding envelope events. — never met as an instance on this machine*

Methods:

- `append((EnvelopeEventVector)arg1, (object)arg2) -> None`
- `extend((EnvelopeEventVector)arg1, (object)arg2) -> None`

---

## Module `Live.Eq8Device`

Enum `EditMode`: a=0, b=1

Enum `GlobalMode`: stereo=0, left_right=1, mid_side=2

### `Eq8Device`
*bases: Device — This class represents an Eq8 device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `edit_mode` | RW | Access to Eq8's edit mode. |
| `global_mode` | RW | Access to Eq8's global mode. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `name` | RW | Return access to the name of the device. |
| `oversample` | RW | Access to Eq8's oversample value. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `edit_mode`, `global_mode`, `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `name`, `oversample`, `parameters`

### `Eq8Device.View`
*bases: View — Representing the view aspects of an Eq8 device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |
| `selected_band` | RW | Access to the selected filter band. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`, `selected_band`

---

## Module `Live.Groove`

Enum `Base`: gb_four=0, gb_eight=1, gb_eight_triplet=2, gb_sixteen=3, gb_sixteen_triplet=4, gb_thirtytwo=5, count=6

### `Groove`
*bases: LomObject — This class represents a groove in Live. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `base` | RW | Get/set the groove's base grid. |
| `canonical_parent` | R | Get the canonical parent of the groove. |
| `name` | RW | Read/write/listen access to the groove's name |
| `quantization_amount` | RW | Read/write/listen access to the groove's quantization amount. |
| `random_amount` | RW | Read/write/listen access to the groove's random amount. |
| `timing_amount` | RW | Read/write/listen access to the groove's timing amount. |
| `velocity_amount` | RW | Read/write/listen access to the groove's velocity amount. |

Listeners (`add_/remove_/…_has_listener`): `name`, `quantization_amount`, `random_amount`, `timing_amount`, `velocity_amount`

---

## Module `Live.GroovePool`

### `GroovePool`
*bases: LomObject — This class represents the groove pool in Live. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the groove pool. |
| `grooves` | R | Access to the list of grooves |

Listeners (`add_/remove_/…_has_listener`): `grooves`

---

## Module `Live.HybridReverbDevice`

### `HybridReverbDevice`
*bases: Device — This class represents a Hybrid Reverb device. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `ir_attack_time` | RW | Return the current IrAttackTime |
| `ir_category_index` | RW | Return the current IR category index |
| `ir_category_list` | R | Return the current IR categories list |
| `ir_decay_time` | RW | Return the current IrDecayTime |
| `ir_file_index` | RW | Return the current IR file index |
| `ir_file_list` | R | Return the current IR file list |
| `ir_size_factor` | RW | Return the current IrSizeFactor |
| `ir_time_shaping_on` | RW | Return the current IrTimeShapingOn |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `ir_attack_time`, `ir_category_index`, `ir_decay_time`, `ir_file_index`, `ir_file_list`, `ir_size_factor`, `ir_time_shaping_on`, `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `name`, `parameters`

### `HybridReverbDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.Licensing`

Module functions:

- `authorization_clock_days_ahead() -> int`
- `get_authorization_page_url((bool)reauthorize, (bool)is_trial) -> str`
- `get_purchase_live_url() -> str`
- `get_services_url() -> str`
- `get_unlock_dir() -> tuple`
- `launch_web_browser((str)url) -> None`

Enum `TrialContext`: SAVE=0, FORCE_UPDATE=2, STARTUP=3

### `ProgressDialog`
*A modal dialog showing a message and a progress animation. — never met as an instance on this machine*

Methods:

- `end_modal_loop((ProgressDialog)arg1) -> None`
- `run_in_modal_loop((ProgressDialog)arg1) -> None`
- `set_status_message((object)arg1, (str)msg) -> None`

### `PythonLicensingBridge`
*Interface to the internal licensing services. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `base_product_id` | R | Returns Live's current base product ID. |
| `in_sassafras_mode` | R |  |
| `license_must_match_variant` | R | Returns a bool indicating if we require the license information returned by the server to match the variant of Live. |
| `random_number_for_trial_authorization` | R | Returns the integer to send along with the Trial authorization request. This same integer will be checked for in `process_trial_response` (and then changed). |
| `set_has_unsaved_changes` | R | Returns true if the set has unsaved changes. |

Methods:

- `authorize_with_sassafras((PythonLicensingBridge)arg1) -> None`
- `create_new_live_set((PythonLicensingBridge)arg1) -> None`
- `deauthenticate_user((PythonLicensingBridge)arg1) -> None`
- `get_progress_dialog((PythonLicensingBridge)arg1) -> ProgressDialog`
- `get_session_id((PythonLicensingBridge)arg1) -> str`
- `get_startup_dialog((PythonLicensingBridge)arg1, (object)authorize_callable, (object)authorize_later_callable) -> StartupDialogServes as an entry point for the user to authorize Live on first launch.`
- `get_trial_time_left((PythonLicensingBridge)arg1) -> str`
- `invoke_pack_installation_callback((PythonLicensingBridge)arg1) -> None`
- `load_and_convert_legacy_unlock_cfg((PythonLicensingBridge)arg1) -> dict`
- `process_license_response((PythonLicensingBridge)arg1, (list)license_response_lines) -> UnlockStatus`
- `process_trial_response((PythonLicensingBridge)arg1, (str)trial_response_line) -> bool`
- `request_exit((PythonLicensingBridge)arg1 [, (int)exit_code=0]) -> None`
- `save_current_set((PythonLicensingBridge)arg1) -> None`
- `set_network_timer((PythonLicensingBridge)arg1, (object)callback, (int)interval_in_ms) -> None`
- `store_session_identifiers((PythonLicensingBridge)arg1, (str)session_id, (str)external_session_id) -> None`

### `StartupDialogServes as an entry point for the user to authorize Live on first launch.`
*never met as an instance on this machine*

Methods:

- `end_modal_loop((StartupDialogServes as an entry point for the user to authorize Live on first launch.)arg1) -> None`
- `run_in_modal_loop((StartupDialogServes as an entry point for the user to authorize Live on first launch.)arg1, (bool)show_only_offline_auth_instructions) -> None`
- `set_notification_message((StartupDialogServes as an entry point for the user to authorize Live on first launch.)arg1, (object)notification_text, (bool)show_progress_bar) -> None`

### `UnlockStatus`
*Returns relevant information after unlock — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `authorization_deactivated` | R |  |
| `authorization_expired` | R |  |
| `has_max_unlock_products` | R |  |
| `temp_demo_mode` | R |  |
| `time_limited` | R |  |
| `unlock_error` | R |  |
| `unlocked` | R |  |

---

## Module `Live.Listener`

### `ListenerHandle`
*This class represents a Python listener when connected to a Live property. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `listener_func` | R | Returns the original function |
| `listener_self` | R | Returns the weak reference to original self, if it was a bound method |
| `name` | R | Prints the name of the property that this listener is connected to |

Methods:

- `disconnect((ListenerHandle)arg1) -> None`

### `ListenerVector`
*A read only container for accessing a list of listeners. — never met as an instance on this machine*

Methods:

- `append((ListenerVector)arg1, (object)arg2) -> None`
- `extend((ListenerVector)arg1, (object)arg2) -> None`

---

## Module `Live.LomObject`

### `LomObject`
*this is the base class for an object that is accessible via the LOM — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |

---

## Module `Live.LooperDevice`

### `LooperDevice`
*bases: Device — This class represents a Looper device. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `loop_length` | R | The length of Looper's buffer. |
| `name` | RW | Return access to the name of the device. |
| `overdub_after_record` | RW | If true, Looper will switch to overdub after recording, when recording a fixed number of bars. Otherwise, the switch will be to playback without overdubbing. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `record_length_index` | RW | Access to the Record Length chooser entry index. |
| `record_length_list` | R | Read-only access to the list of Record Length chooser entry strings. |
| `tempo` | R | The tempo of Looper's buffer. |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `clear((LooperDevice)arg1) -> None`
- `double_length((LooperDevice)arg1) -> None`
- `double_speed((LooperDevice)arg1) -> None`
- `export_to_clip_slot((LooperDevice)arg1, (ClipSlot)arg2) -> None`
- `half_length((LooperDevice)arg1) -> None`
- `half_speed((LooperDevice)arg1) -> None`
- `overdub((LooperDevice)arg1) -> None`
- `play((LooperDevice)arg1) -> None`
- `record((LooperDevice)arg1) -> None`
- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `stop((LooperDevice)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`
- `undo((LooperDevice)arg1) -> None`

Listeners (`add_/remove_/…_has_listener`): `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `loop_length`, `name`, `overdub_after_record`, `parameters`, `record_length_index`, `tempo`

### `LooperDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.MaxDevice`

### `MaxDevice`
*bases: Device — This class represents a Max for Live device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `audio_inputs` | R | Const access to a list of all audio inputs of the device. |
| `audio_outputs` | R | Const access to a list of all audio outputs of the device. |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `midi_inputs` | R | Const access to a list of all midi outputs of the device. |
| `midi_outputs` | R | Const access to a list of all midi outputs of the device. |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `get_bank_count((MaxDevice)arg1) -> int`
- `get_bank_name((MaxDevice)arg1, (int)arg2) -> str`
- `get_bank_parameters((MaxDevice)arg1, (int)arg2) -> list`
- `get_value_item_icons((MaxDevice)arg1, (DeviceParameter)arg2) -> list`
- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `audio_inputs`, `audio_outputs`, `bank_parameters_changed`, `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `midi_inputs`, `midi_outputs`, `name`, `parameters`

### `MaxDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.MeldDevice`

### `MeldDevice`
*bases: Device — This class represents a Meld device. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `mono_poly` | RW | Returns the mode of Polyphony |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `poly_voices` | RW | Return the Poly Voice count |
| `selected_engine` | RW | Return what Voice Engine is selected |
| `type` | R | Return the type of the device. |
| `unison_voices` | RW | Return the Unison Voice count |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `mono_poly`, `name`, `parameters`, `poly_voices`, `selected_engine`, `unison_voices`

### `MeldDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.MidiMap`

Module functions:

- `forward_midi_cc((int)arg1, (int)arg2, (int)arg3, (int)arg4 [, (bool)ShouldConsumeEvent=True]) -> bool`
- `forward_midi_note((int)arg1, (int)arg2, (int)arg3, (int)arg4 [, (bool)ShouldConsumeEvent=True]) -> bool`
- `forward_midi_pitchbend((int)arg1, (int)arg2, (int)arg3) -> bool`
- `map_midi_cc((int)midi_map_handle, (DeviceParameter)parameter, (int)midi_channel, (int)controller_number, (MapMode)map_mode, (bool)avoid_takeover [, (float)sensitivity=1.0]) -> bool`
- `map_midi_cc_with_feedback_map((int)midi_map_handle, (DeviceParameter)parameter, (int)midi_channel, (int)controller_number, (MapMode)map_mode, (CCFeedbackRule)feedback_rule, (bool)avoid_takeover [, (float)sensitivity=1.0]) -> bool`
- `map_midi_note((int)arg1, (DeviceParameter)arg2, (int)arg3, (int)arg4) -> bool`
- `map_midi_note_with_feedback_map((int)arg1, (DeviceParameter)arg2, (int)arg3, (int)arg4, (NoteFeedbackRule)arg5) -> bool`
- `map_midi_pitchbend((int)arg1, (DeviceParameter)arg2, (int)arg3, (bool)arg4) -> bool`
- `map_midi_pitchbend_with_feedback_map((int)arg1, (DeviceParameter)arg2, (int)arg3, (PitchBendFeedbackRule)arg4, (bool)arg5) -> bool`
- `send_feedback_for_parameter((int)arg1, (DeviceParameter)arg2) -> None`

Enum `MapMode`: absolute=0, relative_signed_bit=1, relative_binary_offset=2, relative_two_compliment=3, relative_signed_bit2=4, absolute_14_bit=5, relative_smooth_signed_bit=6, relative_smooth_binary_offset=7, relative_smooth_two_compliment=8, relative_smooth_signed_bit2=9

### `CCFeedbackRule`
*Structure to define feedback properties of MIDI mappings. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `cc_no` | RW |  |
| `cc_value_map` | RW |  |
| `channel` | RW |  |
| `delay_in_ms` | RW |  |
| `enabled` | RW |  |

### `NoteFeedbackRule`
*Structure to define feedback properties of MIDI mappings. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `channel` | RW |  |
| `delay_in_ms` | RW |  |
| `enabled` | RW |  |
| `note_no` | RW |  |
| `vel_map` | RW |  |

### `PitchBendFeedbackRule`
*Structure to define feedback properties of MIDI mappings. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `channel` | RW |  |
| `delay_in_ms` | RW |  |
| `enabled` | RW |  |
| `value_pair_map` | RW |  |

---

## Module `Live.MixerDevice`

### `MixerDevice`
*bases: LomObject — This class represents a Mixer Device in Live, which gives you — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the mixer device. |
| `crossfade_assign` | RW | Player- and ReturnTracks only: Access to the Track's Crossfade Assign State. |
| `crossfader` | R | MainTrack only: Const access to the Crossfader. |
| `cue_volume` | R | MainTrack only: Const access to the Cue Volume Parameter. |
| `left_split_stereo` | R | Const access to the Track's Left Split Stereo Panning Device Parameter. |
| `panning` | R | Const access to the Tracks Panning Device Parameter. |
| `panning_mode` | RW | Access to the Track's Panning Mode. |
| `right_split_stereo` | R | Const access to the Track's Right Split Stereo Panning Device Parameter. |
| `sends` | R | Const access to the Tracks list of Send Amount Device Parameters. |
| `song_tempo` | R | MainTrack only: Const access to the Song's Tempo. |
| `track_activator` | R | Const access to the Tracks Activator Device Parameter. |
| `volume` | R | Const access to the Tracks Volume Device Parameter. |

Enum `MixerDevice.crossfade_assignments`: A=0, NONE=1, B=2

Enum `MixerDevice.panning_modes`: stereo=0, stereo_split=1

Listeners (`add_/remove_/…_has_listener`): `crossfade_assign`, `panning_mode`, `sends`

---

## Module `Live.PluginDevice`

### `PluginDevice`
*bases: Device — This class represents a plugin device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_editor_open` | RW | Access to the opened state of the plugin's editor window. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `presets` | R | Get the list of presets the plugin offers. |
| `selected_preset_index` | RW | Access to the index of the currently selected preset. |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `get_parameter_names((PluginDevice)arg1 [, (int)begin=0 [, (int)end=-1]]) -> StringVector`
- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `is_active`, `is_editor_open`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `name`, `parameters`, `presets`, `selected_preset_index`

### `PluginDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.RackDevice`

### `RackDevice`
*bases: Device — This class represents a Rack device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `can_show_chains` | R | return True, if this Rack contains a rack instrument device that is capable of showing its chains in session view. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `chain_selector` | R | Const access to the chain selector parameter. |
| `chains` | R | Return const access to the list of chains in this device. Throws an exception if can_have_chains is false. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `drum_pads` | R | Return const access to the list of drum pads in this device. Throws an exception if can_have_drum_pads is false. |
| `has_drum_pads` | R | Returns true if the device is a drum rack which has drum pads. Throws an exception if can_have_drum_pads is false. |
| `has_macro_mappings` | R | Returns true if any of the rack's macros are mapped to a parameter. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_showing_chains` | RW | Returns True, if it is showing chains. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `macros_mapped` | R | A list of booleans, one for each macro parameter, which is True iffthat macro is mapped to something |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `return_chains` | R | Return const access to the list of return chains in this device. Throws an exception if can_have_chains is false. |
| `selected_variation_index` | RW | Access to the index of the currently selected macro variation.Throws an exception if the index is out of range. |
| `type` | R | Return the type of the device. |
| `variation_count` | R | Access to the number of macro variations currently stored. |
| `view` | R | Representing the view aspects of a device. |
| `visible_drum_pads` | R | Return const access to the list of visible drum pads in this device. Throws an exception if can_have_drum_pads is false. |
| `visible_macro_count` | R | Access to the number of macros that are currently visible. |

Methods:

- `add_macro((RackDevice)arg1) -> None`
- `copy_pad((RackDevice)arg1, (int)arg2, (int)arg3) -> None`
- `delete_selected_variation((Device)arg1) -> None`
- `insert_chain((RackDevice)arg1 [, (int)Index=-1]) -> LomObject`
- `randomize_macros((RackDevice)arg1) -> None`
- `recall_last_used_variation((Device)arg1) -> None`
- `recall_selected_variation((Device)arg1) -> None`
- `remove_macro((RackDevice)arg1) -> None`
- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`
- `store_variation((Device)arg1) -> None`

Listeners (`add_/remove_/…_has_listener`): `chains`, `drum_pads`, `has_drum_pads`, `has_macro_mappings`, `is_active`, `is_showing_chains`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `macros_mapped`, `name`, `parameters`, `return_chains`, `variation_count`, `visible_drum_pads`, `visible_macro_count`

### `RackDevice.View`
*bases: View — Representing the view aspects of a rack device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `drum_pads_scroll_position` | RW | Access to the index of the lowest visible row of pads. Throws an exception if can_have_drum_pads is false. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |
| `is_showing_chain_devices` | RW | Return whether the devices in the currently selected chain are visible. Throws an exception if can_have_chains is false. |
| `selected_chain` | RW | Return access to the currently selected chain. |
| `selected_drum_pad` | RW | Return access to the currently selected drum pad. Throws an exception if can_have_drum_pads is false. |

Listeners (`add_/remove_/…_has_listener`): `drum_pads_scroll_position`, `is_collapsed`, `is_showing_chain_devices`, `selected_chain`, `selected_drum_pad`

---

## Module `Live.RoarDevice`

### `RoarDevice`
*bases: Device — This class represents a Roar device. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `env_listen` | RW | Return the Envelope Input Listen toggle state |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `routing_mode_index` | RW | Return the routing mode index |
| `routing_mode_list` | R | Return the routing mode list |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `env_listen`, `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `name`, `parameters`, `routing_mode_index`

### `RoarDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.Sample`

Enum `SlicingBeatDivision`: sixteenth=0, sixteenth_triplett=1, eighth=2, eighth_triplett=3, quarter=4, quarter_triplett=5, half=6, half_triplett=7, one_bar=8, two_bars=9, four_bars=10

Enum `SlicingStyle`: transient=0, beat=1, region=2, manual=3

Enum `TransientLoopMode`: off=0, forward=1, alternate=2

### `Sample`
*bases: LomObject — This class represents a sample file loaded into a Simpler instance. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `beats_granulation_resolution` | RW | Access to the Granulation Resolution parameter in Beats Warp Mode. |
| `beats_transient_envelope` | RW | Access to the Transient Envelope parameter in Beats Warp Mode. |
| `beats_transient_loop_mode` | RW | Access to the Transient Loop Mode parameter in Beats Warp Mode. |
| `canonical_parent` | R | Access to the sample's canonical parent. |
| `complex_pro_envelope` | RW | Access to the Envelope parameter in Complex Pro Mode. |
| `complex_pro_formants` | RW | Access to the Formants parameter in Complex Pro Warp Mode. |
| `end_marker` | RW | Access to the position of the sample's end marker. |
| `file_path` | R | Get the path of the sample file. |
| `gain` | RW | Access to the sample gain. |
| `length` | R | Get the length of the sample file in sample frames. |
| `sample_rate` | R | Access to the audio sample rate of the sample. |
| `slices` | R | Access to the list of slice points in sample time in the sample. |
| `slicing_beat_division` | RW | Access to sample's slicing step size. |
| `slicing_region_count` | RW | Access to sample's slicing split count. |
| `slicing_sensitivity` | RW | Access to sample's slicing sensitivity whose sensitivity is in between 0.0 and 1.0. |
| `slicing_style` | RW | Access to sample's slicing style. |
| `start_marker` | RW | Access to the position of the sample's start marker. |
| `texture_flux` | RW | Access to the Flux parameter in Texture Warp Mode. |
| `texture_grain_size` | RW | Access to the Grain Size parameter in Texture Warp Mode. |
| `tones_grain_size` | RW | Access to the Grain Size parameter in Tones Warp Mode. |
| `warp_markers` | R | Get the warp markers for this sample. |
| `warp_mode` | RW | Access to the sample's warp mode. |
| `warping` | RW | Access to the sample's warping property. |

Methods:

- `beat_to_sample_time((Sample)self, (float)beat_time) -> float`
- `clear_slices((Sample)self) -> None`
- `gain_display_string((Sample)self) -> str`
- `insert_slice((Sample)self, (int)slice_time) -> None`
- `move_slice((Sample)self, (int)old_time, (int)new_time) -> int`
- `remove_slice((Sample)self, (int)slice_time) -> None`
- `reset_slices((Sample)self) -> None`
- `sample_to_beat_time((Sample)self, (float)sample_time) -> float`

Listeners (`add_/remove_/…_has_listener`): `beats_granulation_resolution`, `beats_transient_envelope`, `beats_transient_loop_mode`, `complex_pro_envelope`, `complex_pro_formants`, `end_marker`, `file_path`, `gain`, `slices`, `slicing_beat_division`, `slicing_region_count`, `slicing_sensitivity`, `slicing_style`, `start_marker`, `texture_flux`, `texture_grain_size`, `tones_grain_size`, `warp_markers`, `warp_mode`, `warping`

---

## Module `Live.Scene`

### `Scene`
*bases: LomObject — This class represents an series of ClipSlots in Lives Sessionview matrix. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the scene. |
| `clip_slots` | R | return a list of clipslots (see class AClipSlot) that this scene covers. |
| `color` | RW | Get/set access to the color of the scene (RGB). |
| `color_index` | RW | Get/set access to the color index of the scene. Can be None for no color. |
| `is_empty` | R | Returns True if all clip slots of this scene are empty. |
| `is_triggered` | R | Const access to the scene's trigger state. |
| `name` | RW | Get/Set the name of the scene. |
| `tempo` | RW | Get/Set the tempo value of the scene. |
| `tempo_enabled` | RW | Get/Set the active state of the scene tempo. |
| `time_signature_denominator` | RW | Get/Set the scene's time signature denominator. |
| `time_signature_enabled` | RW | Get the active state of the scene time signature. |
| `time_signature_numerator` | RW | Get/Set the scene's time signature numerator. |

Methods:

- `fire((Scene)arg1 [, (bool)force_legato=False [, (bool)can_select_scene_on_launch=True]]) -> None`
- `fire_as_selected((Scene)arg1 [, (bool)force_legato=False]) -> None`
- `set_fire_button_state((Scene)arg1, (bool)arg2) -> None`

Listeners (`add_/remove_/…_has_listener`): `clip_slots`, `color`, `color_index`, `is_triggered`, `name`, `tempo`, `tempo_enabled`, `time_signature_denominator`, `time_signature_enabled`, `time_signature_numerator`

---

## Module `Live.ShifterDevice`

### `ShifterDevice`
*bases: Device — This class represents a Shifter device. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `pitch_bend_range` | RW | Return the pitch bend range for MIDI pitch mode |
| `pitch_mode_index` | RW | Return the current pitch mode index |
| `pitch_mode_list` | R | Return the current pitch mode list |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `name`, `parameters`, `pitch_bend_range`, `pitch_mode_index`

### `ShifterDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.SimplerDevice`

Module functions:

- `get_available_voice_numbers() -> IntVector`

Enum `PlaybackMode`: classic=0, one_shot=1, slicing=2

Enum `SlicingPlaybackMode`: mono=0, poly=1, thru=2

### `SimplerDevice`
*bases: Device — This class represents a Simpler device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `can_warp_as` | R | Returns true if warp_as is available. |
| `can_warp_double` | R | Returns true if warp_double is available. |
| `can_warp_half` | R | Returns true if warp_half is available. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `multi_sample_mode` | R | Returns whether Simpler is in mulit-sample mode. |
| `name` | RW | Return access to the name of the device. |
| `note_pitch_bend_range` | RW | Access to the Note Pitch Bend Range in Simpler. |
| `pad_slicing` | RW | When set to true, slices can be added in slicing mode by playing notes |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `pitch_bend_range` | RW | Access to the Pitch Bend Range in Simpler. |
| `playback_mode` | RW | Access to Simpler's playback mode. |
| `playing_position` | R | Constant access to the current playing position in the sample. |
| `playing_position_enabled` | R | Returns whether Simpler is showing the playing position. |
| `retrigger` | RW | Access to Simpler's retrigger mode. |
| `sample` | R | Get the loaded Sample. |
| `slicing_playback_mode` | RW | Access to Simpler's slicing playback mode. |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |
| `voices` | RW | Access to the number of voices in Simpler. |

Methods:

- `crop((SimplerDevice)self) -> None`
- `guess_playback_length((SimplerDevice)self) -> float`
- `replace_sample((SimplerDevice)self, (object)file_path) -> None`
- `reverse((SimplerDevice)self) -> None`
- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`
- `warp_as((SimplerDevice)self, (float)beat_time) -> None`
- `warp_double((SimplerDevice)self) -> None`
- `warp_half((SimplerDevice)self) -> None`

Listeners (`add_/remove_/…_has_listener`): `can_warp_as`, `can_warp_double`, `can_warp_half`, `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `multi_sample_mode`, `name`, `note_pitch_bend_range`, `pad_slicing`, `parameters`, `pitch_bend_range`, `playback_mode`, `playing_position`, `playing_position_enabled`, `retrigger`, `sample`, `slicing_playback_mode`, `voices`

### `SimplerDevice.View`
*bases: View — Representing the view aspects of a simpler device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |
| `sample_end` | R | Access to the modulated samples end position in samples. Returns -1 in case there is no sample loaded. |
| `sample_env_fade_in` | R | Access to the envelope fade-in time in samples. Returned value is only in use when Simpler is in one-shot mode. Returns -1 in case there is no sample loaded. |
| `sample_env_fade_out` | R | Access to the envelope fade-out time in samples. Returned value is only in use when Simpler is in one-shot mode. Returns -1 in case there is no sample loaded. |
| `sample_loop_end` | R | Access to the modulated samples loop end position in samples. Returns -1 in case there is no sample loaded. |
| `sample_loop_fade` | R | Access to the modulated samples loop fade position in samples. Returns -1 in case there is no sample loaded. |
| `sample_loop_start` | R | Access to the modulated samples loop start position in samples. Returns -1 in case there is no sample loaded. |
| `sample_start` | R | Access to the modulated samples start position in samples. Returns -1 in case there is no sample loaded. |
| `selected_slice` | RW | Access to the selected slice. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`, `sample_end`, `sample_env_fade_in`, `sample_env_fade_out`, `sample_loop_end`, `sample_loop_fade`, `sample_loop_start`, `sample_start`, `selected_slice`

---

## Module `Live.Song`

Module functions:

- `get_all_scales_ordered() -> tuple`

Enum `CaptureDestination`: auto=0, session=1, arrangement=2

Enum `CaptureMode`: all=0, all_except_selected=1

Enum `Quantization`: q_no_q=0, q_8_bars=1, q_4_bars=2, q_2_bars=3, q_bar=4, q_half=5, q_half_triplet=6, q_quarter=7, q_quarter_triplet=8, q_eight=9, q_eight_triplet=10, q_sixtenth=11, q_sixtenth_triplet=12, q_thirtytwoth=13

Enum `RecordingQuantization`: rec_q_no_q=0, rec_q_quarter=1, rec_q_eight=2, rec_q_eight_triplet=3, rec_q_eight_eight_triplet=4, rec_q_sixtenth=5, rec_q_sixtenth_triplet=6, rec_q_sixtenth_sixtenth_triplet=7, rec_q_thirtysecond=8

Enum `SessionRecordStatus`: off=0, on=1, transition=2

Enum `TimeFormat`: ms_time=0, smpte_24=1, smpte_25=2, smpte_30=3, smpte_30_drop=4, smpte_29=5

### `BeatTime`
*Represents a Time, splitted into Bars, Beats, SubDivision and Ticks. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `bars` | RW |  |
| `beats` | RW |  |
| `sub_division` | RW |  |
| `ticks` | RW |  |

### `CuePoint`
*bases: LomObject — Represents a 'Marker' in the arrangement. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the cue point. |
| `name` | RW | Get/Set/Listen to the name of this CuePoint, as visible in the arranger. |
| `time` | R | Get/Listen to the CuePoint's time in beats. |

Methods:

- `jump((CuePoint)arg1) -> None`

Listeners (`add_/remove_/…_has_listener`): `name`, `time`

### `SmptTime`
*Represents a Time, split into Hours, Minutes, Seconds and Frames. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `frames` | RW |  |
| `hours` | RW |  |
| `minutes` | RW |  |
| `seconds` | RW |  |

### `Song`
*bases: LomObject — This class represents a Live set. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `appointed_device` | RW | Read, write, and listen access to the appointed Device |
| `arrangement_overdub` | RW | Get/Set the global arrangement overdub state. |
| `back_to_arranger` | RW | Get/Set if triggering a Clip in the Session, disabled the playback of |
| `can_capture_midi` | R | Get whether there currently is material to be captured on any tracks. |
| `can_jump_to_next_cue` | R | Returns true when there is a cue marker right to the playing pos that |
| `can_jump_to_prev_cue` | R | Returns true when there is a cue marker left to the playing pos that |
| `can_redo` | R | Returns true if there is an undone action that we can redo. |
| `can_undo` | R | Returns true if there is an action that we can restore. |
| `canonical_parent` | R | Get the canonical parent of the song. |
| `clip_trigger_quantization` | RW | Get/Set access to the quantization settings that are used to fire |
| `count_in_duration` | R | Get the count in duration. Returns an index, mapped as follows: |
| `cue_points` | R | Const access to a list of all cue points of the Live Song. |
| `current_song_time` | RW | Get/Set access to the songs current playing position in beats. |
| `exclusive_arm` | R | Get if Tracks should be armed exclusively by default. |
| `exclusive_solo` | R | Get if Tracks should be soloed exclusively by default. |
| `file_path` | R | Get the current Live Set's path on disk. |
| `groove_amount` | RW | Get/Set the global groove amount, that adjust all setup grooves |
| `groove_pool` | R | Get the groove pool. |
| `is_ableton_link_enabled` | RW | Enable/disable Ableton Link. |
| `is_ableton_link_start_stop_sync_enabled` | RW | Enable/disable Ableton Link Start Stop Sync. |
| `is_counting_in` | R | Get whether currently counting in. |
| `is_playing` | RW | Returns true if the Song is currently playing. |
| `last_event_time` | R | Return the time of the last set event in the song. In contrary to |
| `loop` | RW | Get/Set the looping flag that en/disables the usage of the global |
| `loop_length` | RW | Get/Set the length of the global loop marker position in beats. |
| `loop_start` | RW | Get/Set the start of the global loop marker position in beats. |
| `master_track` | R | Access to the Main Track (always available) |
| `metronome` | RW | Get/Set if the metronom is audible. |
| `midi_recording_quantization` | RW | Get/Set access to the settings that are used to quantize |
| `name` | R | Get the current Live Set's name. |
| `nudge_down` | RW | Get/Set the status of the nudge down button. |
| `nudge_up` | RW | Get/Set the status of the nudge up button. |
| `overdub` | RW | Legacy hook for Live 8 overdub state. Now hooks to |
| `punch_in` | RW | Get/Set the flag that will enable recording as soon as the Song plays |
| `punch_out` | RW | Get/Set the flag that will disable recording as soon as the Song plays |
| `re_enable_automation_enabled` | R | Returns true if some automated parameter has been overriden |
| `record_mode` | RW | Get/Set the state of the global recording flag. |
| `return_tracks` | R | Const access to the list of available Return Tracks. |
| `root_note` | RW | Set and access the root (i.e. key) of the song. The root can be a number between 0 and 11, with 0 corresponding to C and 11 corresponding to B. |
| `scale_intervals` | R | Reports the current scale's intervals as a list of integers, starting with the root and representing the number of halfsteps (e.g. Major -> 0, 2, 4, 5, 7, 9, 11) |
| `scale_mode` | RW | Access to the Scale Mode setting in Live. When on, key tracks that belong to the currently selected scale are highlighted in Live's MIDI Note Editor, and pitch-based parameters in MIDI Tools and Devices can be edited in scale degrees rather than semitones. |
| `scale_name` | RW | Set and access the currently selected scale by name. The default scale names that can be saved with a set and recalled are |
| `scenes` | R | Const access to a list of all Scenes in the Live Song. |
| `select_on_launch` | R | Get if Scenes and Clips should be selected when fired. |
| `session_automation_record` | RW | Returns true if automation recording is enabled. |
| `session_record` | RW | Get/Set the session record state. |
| `session_record_status` | R | Get the session slot-recording state. |
| `signature_denominator` | RW | Get/Set access to the global signature denominator of the Song. |
| `signature_numerator` | RW | Get/Set access to the global signature numerator of the Song. |
| `song_length` | R | Return the time of the last set event in the song, plus som extra beats |
| `start_time` | RW | Get/Set access to the songs current start time in beats. The set time |
| `swing_amount` | RW | Get/Set access to the amount of swing that is applied when adding or quantizing notes to MIDI clips |
| `tempo` | RW | Get/Set the global project tempo. |
| `tempo_follower_enabled` | RW | Get/Set whether the Tempo Follower is controlling the tempo. The Tempo Follower Toggle must be made visible in the preferences for this property to be effective. |
| `tracks` | R | Const access to a list of all Player Tracks in the Live Song, excluding |
| `tuning_system` | R | Access the currently active tuning system. |
| `view` | R | Representing the view aspects of a Live document: |
| `visible_tracks` | R | Const access to a list of all visible Player Tracks in the Live Song, excluding |

Methods:

- `begin_undo_step((Song)arg1) -> None`
- `capture_and_insert_scene((Song)arg1 [, (int)CaptureMode=Song.CaptureMode.all]) -> None`
- `capture_midi((Song)arg1 [, (int)Destination=Song.CaptureDestination.auto]) -> None`
- `continue_playing((Song)arg1) -> None`
- `create_audio_track((Song)arg1 [, (object)Index=None]) -> Track`
- `create_midi_track((Song)arg1 [, (object)Index=None]) -> Track`
- `create_return_track((Song)arg1) -> Track`
- `create_scene((Song)arg1, (int)arg2) -> Scene`
- `delete_return_track((Song)arg1, (int)arg2) -> None`
- `delete_scene((Song)arg1, (int)arg2) -> None`
- `delete_track((Song)arg1, (int)arg2) -> None`
- `duplicate_scene((Song)arg1, (int)arg2) -> None`
- `duplicate_track((Song)arg1, (int)arg2) -> None`
- `end_undo_step((Song)arg1) -> None`
- `find_device_position((Song)arg1, (Device)device, (LomObject)target, (int)target_position) -> int`
- `force_link_beat_time((Song)arg1) -> None`
- `get_beats_loop_length((Song)arg1) -> BeatTime`
- `get_beats_loop_start((Song)arg1) -> BeatTime`
- `get_current_beats_song_time((Song)arg1) -> BeatTime`
- `get_current_smpte_song_time((Song)arg1, (int)arg2) -> SmptTime`
- `get_data((Song)arg1, (object)key, (object)default_value) -> object`
- `is_cue_point_selected((Song)arg1) -> bool`
- `jump_by((Song)arg1, (float)arg2) -> None`
- `jump_to_next_cue((Song)arg1) -> None`
- `jump_to_prev_cue((Song)arg1) -> None`
- `move_device((Song)arg1, (Device)device, (LomObject)target, (int)target_position) -> int`
- `play_selection((Song)arg1) -> None`
- `re_enable_automation((Song)arg1) -> None`
- `redo((Song)arg1) -> str`
- `scrub_by((Song)arg1, (float)arg2) -> None`
- `set_data((Song)arg1, (object)key, (object)value) -> None`
- `set_or_delete_cue((Song)arg1) -> None`
- `start_playing((Song)arg1) -> None`
- `stop_all_clips((Song)arg1 [, (bool)Quantized=True]) -> None`
- `stop_playing((Song)arg1) -> None`
- `sync_parameter_changes((Song)arg1) -> None`
- `tap_tempo((Song)arg1) -> None`
- `trigger_session_record((Song)self [, (float)record_length=1.7976931348623157e+308]) -> None`
- `undo((Song)arg1) -> str`

Listeners (`add_/remove_/…_has_listener`): `appointed_device`, `arrangement_overdub`, `back_to_arranger`, `can_capture_midi`, `can_jump_to_next_cue`, `can_jump_to_prev_cue`, `clip_trigger_quantization`, `count_in_duration`, `cue_points`, `current_song_time`, `data`, `exclusive_arm`, `groove_amount`, `is_ableton_link_enabled`, `is_ableton_link_start_stop_sync_enabled`, `is_counting_in`, `is_playing`, `loop`, `loop_length`, `loop_start`, `metronome`, `midi_recording_quantization`, `nudge_down`, `nudge_up`, `overdub`, `punch_in`, `punch_out`, `re_enable_automation_enabled`, `record_mode`, `return_tracks`, `root_note`, `scale_information`, `scale_intervals`, `scale_mode`, `scale_name`, `scenes`, `session_automation_record`, `session_record`, `session_record_status`, `signature_denominator`, `signature_numerator`, `song_length`, `start_time`, `swing_amount`, `tempo`, `tempo_follower_enabled`, `tracks`, `tuning_system`, `visible_tracks`

### `Song.View`
*bases: LomObject — Representing the view aspects of a Live document: The Session and Arrangerview. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the song view. |
| `detail_clip` | RW | Get/Set the Clip that is currently visible in Lives Detailview. |
| `draw_mode` | RW | Get/Set if the Envelope/Note draw mode is enabled. |
| `follow_song` | RW | Get/Set if the Arrangerview should scroll to show the playmarker. |
| `highlighted_clip_slot` | RW | Get/Set the clip slot, defined via the selected track and scene in the Session.Will be None for Main- and Sendtracks. |
| `mod_mapping_device` | RW | The device that is waiting for a parameter (via mod_mapping_parameter) to modulate, or None if no device is waiting. |
| `mod_mapping_parameter` | R | Get the device parameter that's current selected to be mapped. |
| `selected_chain` | RW | Get the highlighted chain if available. |
| `selected_parameter` | R | Get the currently selected device parameter. |
| `selected_scene` | RW | Get/Set the current selected scene in Lives Sessionview. |
| `selected_track` | RW | Get/Set the current selected Track in Lives Session or Arrangerview. |

Methods:

- `select_device((View)arg1, (Device)arg2 [, (bool)ShouldAppointDevice=True]) -> None`

Listeners (`add_/remove_/…_has_listener`): `detail_clip`, `draw_mode`, `follow_song`, `mod_mapping_device`, `mod_mapping_parameter`, `selected_chain`, `selected_parameter`, `selected_scene`, `selected_track`

---

## Module `Live.SpectralResonatorDevice`

### `SpectralResonatorDevice`
*bases: Device — This class represents a Spectral Resonator device. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `frequency_dial_mode` | RW | Return the current frequency dial mode index |
| `frequency_dial_mode_list` | R | Return the current frequency dial mode list |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `midi_gate` | RW | Return the current midi gate index |
| `midi_gate_list` | R | Return the current midi gate list |
| `mod_mode` | RW | Return the current mod mode index |
| `mod_mode_list` | R | Return the current mod mode list |
| `mono_poly` | RW | Return the current mono poly mode index |
| `mono_poly_list` | R | Return the current mono poly mode list |
| `name` | RW | Return access to the name of the device. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `pitch_bend_range` | RW | Return the current pitch bend range |
| `pitch_mode` | RW | Return the current pitch mode index |
| `pitch_mode_list` | R | Return the current pitch mode list |
| `polyphony` | RW | Return the current polyphony |
| `type` | R | Return the type of the device. |
| `view` | R | Representing the view aspects of a device. |

Methods:

- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `frequency_dial_mode`, `frequency_dial_mode_list`, `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `midi_gate`, `midi_gate_list`, `mod_mode`, `mod_mode_list`, `mono_poly`, `mono_poly_list`, `name`, `parameters`, `pitch_bend_range`, `pitch_mode`, `pitch_mode_list`, `polyphony`

### `SpectralResonatorDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

---

## Module `Live.TakeLane`

### `TakeLane`
*bases: LomObject — This class represents a take lane in Live. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `arrangement_clips` | R | Read-only access to the arrangement clips in the take lane. |
| `canonical_parent` | R | Get the canonical parent of the take lane. |
| `name` | RW | Read/write access to the name of the TakeLane, as visible in the take lane header. |

Methods:

- `create_audio_clip((TakeLane)arg1, (object)arg2, (float)arg3) -> Clip`
- `create_midi_clip((TakeLane)arg1, (float)arg2, (float)arg3) -> Clip`

Listeners (`add_/remove_/…_has_listener`): `arrangement_clips`, `name`

---

## Module `Live.Track`

Enum `DeviceInsertMode`: default=0, selected_left=1, selected_right=2, count=3

Enum `RoutingChannelLayout`: midi=0, mono=1, stereo=2

Enum `RoutingTypeCategory`: external=0, rewire=1, resampling=2, master=3, track=4, parent_group_track=5, none=6, invalid=7

### `DeviceContainer`
*bases: LomObject — This class is a common super class of Track and Chain — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |

### `RoutingChannel`
*This class represents a routing channel. — seen live*

| property | writable | doc |
|---|---|---|
| `display_name` | R | Display name of routing channel. |
| `layout` | R | The routing channel's Layout, e.g., mono or stereo. |

### `RoutingChannelVector`
*A container for returning routing channels from Live. — never met as an instance on this machine*

Methods:

- `append((RoutingChannelVector)arg1, (object)arg2) -> None`
- `extend((RoutingChannelVector)arg1, (object)arg2) -> None`

### `RoutingType`
*This class represents a routing type. — seen live*

| property | writable | doc |
|---|---|---|
| `attached_object` | R | Live object associated with the routing type. |
| `category` | R | Category of the routing type. |
| `display_name` | R | Display name of routing type. |

### `RoutingTypeVector`
*A container for returning routing types from Live. — never met as an instance on this machine*

Methods:

- `append((RoutingTypeVector)arg1, (object)arg2) -> None`
- `extend((RoutingTypeVector)arg1, (object)arg2) -> None`

### `Track`
*bases: DeviceContainer — This class represents a track in Live. It can be either an Audio — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `arm` | RW | Arm the track for recording. Not available for Main- and Send Tracks. |
| `arrangement_clips` | R | const access to the list of clips in arrangement viewThe list will be empty for the main, send and group tracks. |
| `available_input_routing_channels` | R | Return a list of source channels for input routing. |
| `available_input_routing_types` | R | Return a list of source types for input routing. |
| `available_output_routing_channels` | R | Return a list of destination channels for output routing. |
| `available_output_routing_types` | R | Return a list of destination types for output routing. |
| `back_to_arranger` | RW | Indicates if it's possible to go back to playing back the clips in the Arranger.Setting a value 0 will go back to the Arranger playback. Setting on grouptracks will go back to the Arranger on all grouped tracks. |
| `can_be_armed` | R | return True, if this Track has a valid arm property. Not all tracks |
| `can_be_frozen` | R | return True, if this Track can be frozen. |
| `can_show_chains` | R | return True, if this Track contains a rack instrument device that is capable of showing its chains in session view. |
| `canonical_parent` | R | Get the canonical parent of the track. |
| `clip_slots` | R | const access to the list of clipslots (see class AClipSlot) for this track. |
| `color` | RW | Get/set access to the color of the Track (RGB). |
| `color_index` | RW | Get/Set access to the color index of the track. Can be None for no color. |
| `current_input_routing` | RW | Get/Set the name of the current active input routing. |
| `current_input_sub_routing` | RW | Get/Set the current active input sub routing. |
| `current_monitoring_state` | RW | Get/Set the track's current monitoring state. |
| `current_output_routing` | RW | Get/Set the current active output routing. |
| `current_output_sub_routing` | RW | Get/Set the current active output sub routing. |
| `devices` | R | Return const access to all available Devices that are present in the Tracks |
| `fired_slot_index` | R | const access to the index of the fired (and thus blinking) clipslot in this track. |
| `fold_state` | RW | Get/Set whether the track is folded or not. Only available if is_foldable is True. |
| `group_track` | R | return the group track if is_grouped. |
| `has_audio_input` | R | return True, if this Track can be feed with an Audio signal. This is |
| `has_audio_output` | R | return True, if this Track sends out an Audio signal. This is |
| `has_midi_input` | R | return True, if this Track can be feed with an Audio signal. This is |
| `has_midi_output` | R | return True, if this Track sends out MIDI events. This is |
| `implicit_arm` | RW | Arm the track for recording. When The track is implicitly armed, it showsin a weaker color in the live GUI and is not saved in the set. |
| `input_meter_left` | R | Momentary value of left input channel meter, 0.0 to 1.0. For Audio Tracks only. |
| `input_meter_level` | R | Return the MIDI or Audio meter value of the Tracks input, depending on the |
| `input_meter_right` | R | Momentary value of right input channel meter, 0.0 to 1.0. For Audio Tracks only. |
| `input_routing_channel` | RW | Get and set the current source channel for input routing. |
| `input_routing_type` | RW | Get and set the current source type for input routing. |
| `input_routings` | R | Const access to the list of available input routings. |
| `input_sub_routings` | R | Return a list of all available input sub routings. |
| `is_foldable` | R | return True if the track can be (un)folded to hide/reveal contained tracks. |
| `is_frozen` | R | return True if this Track is currently frozen. No changes should be applied to the track's devices or clips while it is frozen. |
| `is_grouped` | R | return True if this Track is current part of a group track. |
| `is_part_of_selection` | R | return False if the track is not selected. |
| `is_showing_chains` | RW | Get/Set whether a track with a rack device is showing its chains in session view. |
| `is_visible` | R | return False if the track is hidden within a folded group track. |
| `mixer_device` | R | Return access to the special Device that every Track has: This Device contains |
| `mute` | RW | Mute/unmute the track. |
| `muted_via_solo` | R | Returns true if the track is muted because another track is soloed. |
| `name` | RW | Read/write access to the name of the Track, as visible in the track header. |
| `output_meter_left` | R | Momentary value of left output channel meter, 0.0 to 1.0. |
| `output_meter_level` | R | Return the MIDI or Audio meter value of the Track output (behind the |
| `output_meter_right` | R | Momentary value of right output channel meter, 0.0 to 1.0. |
| `output_routing_channel` | RW | Get and set the current destination channel for output routing. |
| `output_routing_type` | RW | Get and set the current destination type for output routing. |
| `output_routings` | R | Const access to the list of all available output routings. |
| `output_sub_routings` | R | Return a list of all available output sub routings. |
| `performance_impact` | R | Reports the performance impact of this track. |
| `playing_slot_index` | R | const access to the index of the currently playing clip in the track. |
| `solo` | RW | Get/Set the solo status of the track. Note that this will not disable the |
| `take_lanes` | R | returns the take lanes. |
| `view` | R | Representing the view aspects of a Track. |

Methods:

- `create_audio_clip((Track)arg1, (object)arg2, (float)arg3) -> Clip`
- `create_midi_clip((Track)arg1, (float)arg2, (float)arg3) -> Clip`
- `create_take_lane((Track)arg1) -> LomObject`
- `delete_clip((Track)arg1, (Clip)arg2) -> None`
- `delete_device((Track)arg1, (int)arg2) -> None`
- `duplicate_clip_slot((Track)arg1, (int)arg2) -> int`
- `duplicate_clip_to_arrangement((Track)self, (Clip)clip, (float)destination_time) -> Clip`
- `duplicate_device((Track)arg1, (int)arg2) -> None`
- `get_data((Track)arg1, (object)key, (object)default_value) -> object`
- `insert_device((Track)arg1, (str)DeviceName [, (int)DeviceIndex=-1]) -> LomObject`
- `jump_in_running_session_clip((Track)arg1, (float)arg2) -> None`
- `set_data((Track)arg1, (object)key, (object)value) -> None`
- `stop_all_clips((Track)arg1 [, (bool)Quantized=True]) -> None`

Enum `Track.monitoring_states`: IN=0, AUTO=1, OFF=2

Listeners (`add_/remove_/…_has_listener`): `arm`, `arrangement_clips`, `available_input_routing_channels`, `available_input_routing_types`, `available_output_routing_channels`, `available_output_routing_types`, `back_to_arranger`, `clip_slots`, `color`, `color_index`, `current_input_routing`, `current_input_sub_routing`, `current_monitoring_state`, `current_output_routing`, `current_output_sub_routing`, `data`, `devices`, `fired_slot_index`, `has_audio_input`, `has_audio_output`, `has_midi_input`, `has_midi_output`, `implicit_arm`, `input_meter_left`, `input_meter_level`, `input_meter_right`, `input_routing_channel`, `input_routing_type`, `input_routings`, `input_sub_routings`, `is_frozen`, `is_showing_chains`, `mute`, `muted_via_solo`, `name`, `output_meter_left`, `output_meter_level`, `output_meter_right`, `output_routing_channel`, `output_routing_type`, `output_routings`, `output_sub_routings`, `performance_impact`, `playing_slot_index`, `solo`, `take_lanes`

### `Track.View`
*bases: LomObject — Representing the view aspects of a Track. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the track view. |
| `device_insert_mode` | RW | Get/Listen the device insertion mode of the track. By default, it will insert devices at the end, but it can be changed to make it relative to current selection. |
| `is_collapsed` | RW | Get/Set/Listen if the track is shown collapsed in the arranger view. |
| `selected_device` | R | Get/Set/Listen the insertion mode of the device. While in insertion mode, loading new devices from the browser will place devices at the selected position. |

Methods:

- `select_instrument((View)arg1) -> bool`

Listeners (`add_/remove_/…_has_listener`): `device_insert_mode`, `is_collapsed`, `selected_device`

---

## Module `Live.TuningSystem`

### `PitchClassAndOctave`
*This class represents a PitchClassAndOctave type. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `index_in_octave` | R | A PitchClassAndOctave's index within the pseudo octave. |
| `octave` | R | A PitchClassAndOctave's octave. |

### `ReferencePitch`
*This class represents a ReferencePitch type. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `frequency` | R | A ReferencePitch's frequency in Hz. |
| `index_in_octave` | R | A ReferencePitch's index within the pseudo octave. |
| `octave` | R | A ReferencePitch's octave. |

### `TuningSystem`
*bases: LomObject — Represents a Tuning System and its properties. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the TuningSystem. |
| `highest_note` | RW | Get/Set the highest note of the current tuning system, where the first entry is |
| `lowest_note` | RW | Get/Set the lowest note of the current tuning system, where the first entry is |
| `name` | RW | Get/Set the name of the currently active tuning system. |
| `note_tunings` | RW | Get/Set the currently active tuning system's note tunings, specified in Cents, where 100 Cents is one semi-tone in equal temperament. |
| `number_of_notes_in_pseudo_octave` | R | Get the number of notes in the pseudo octave. |
| `pseudo_octave_in_cents` | R | Get the pseudo octave in cents for the currently active tuning system. |
| `reference_pitch` | RW | Get/Set the reference pitch the currently active tuning system. |

Listeners (`add_/remove_/…_has_listener`): `highest_note`, `lowest_note`, `name`, `note_tunings`, `reference_pitch`

---

## Module `Live.WavetableDevice`

Enum `EffectMode`: none=0, frequency_modulation=1, sync_and_pulse_width=2, warp_and_fold=3

Enum `FilterRouting`: serial=0, parallel=1, split=2

Enum `ModulationSource`: amp_envelope=0, envelope_2=1, envelope_3=2, lfo_1=3, lfo_2=4, midi_velocity=5, midi_note=6, midi_pitch_bend=7, midi_channel_pressure=8, midi_mod_wheel=9, midi_random=10

Enum `UnisonMode`: none=0, classic=1, slow_shimmer=2, fast_shimmer=3, phase_sync=4, position_spread=5, random_note=6

Enum `VoiceCount`: two=0, three=1, four=2, five=3, six=4, seven=5, eight=6, sixteen=7

Enum `Voicing`: mono=0, poly=1

### `WavetableDevice`
*bases: Device — This class represents a Wavetable device. — never met as an instance on this machine*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `can_compare_ab` | R | Returns true if the Device has the capability to AB compare. |
| `can_have_chains` | R | Returns true if the device is a rack. |
| `can_have_drum_pads` | R | Returns true if the device is a drum rack. |
| `canonical_parent` | R | Get the canonical parent of the Device. |
| `class_display_name` | R | Return const access to the name of the device's class name as displayed in Live's browser and device chain |
| `class_name` | R | Return const access to the name of the device's class. |
| `filter_routing` | RW | Return the current filter routing. |
| `is_active` | R | Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off. |
| `is_using_compare_preset_b` | RW | Returns whether the Device has loaded the preset in compare slot B. Only relevant if can_compare_ab, otherwise errors. |
| `latency_in_ms` | R | Returns the latency of the device in ms. |
| `latency_in_samples` | R | Returns the latency of the device in samples. |
| `mono_poly` | RW | Return the current voicing mode. |
| `name` | RW | Return access to the name of the device. |
| `oscillator_1_effect_mode` | RW | Return the current effect mode of the oscillator 1. |
| `oscillator_1_wavetable_category` | RW | Return the current wavetable category of the oscillator 1. |
| `oscillator_1_wavetable_index` | RW | Return the current wavetable index of the oscillator 1. |
| `oscillator_1_wavetables` | R | Get a vector of oscillator 1's wavetable names. |
| `oscillator_2_effect_mode` | RW | Return the current effect mode of the oscillator 2. |
| `oscillator_2_wavetable_category` | RW | Return the current wavetable category of the oscillator 2. |
| `oscillator_2_wavetable_index` | RW | Return the current wavetable index of the oscillator 2. |
| `oscillator_2_wavetables` | R | Get a vector of oscillator 2's wavetable names. |
| `oscillator_wavetable_categories` | R | Get a vector of the available wavetable categories. |
| `parameters` | R | Const access to the list of available automatable parameters for this device. |
| `poly_voices` | RW | Return the current number of polyphonic voices. Uses the VoiceCount enumeration. |
| `type` | R | Return the type of the device. |
| `unison_mode` | RW | Return the current unison mode. |
| `unison_voice_count` | RW | Return the current number of unison voices. |
| `view` | R | Representing the view aspects of a device. |
| `visible_modulation_target_names` | R | Get the names of all the visible modulation targets. |

Methods:

- `add_parameter_to_modulation_matrix((WavetableDevice)self, (DeviceParameter)parameter) -> int`
- `get_modulation_target_parameter_name((WavetableDevice)self, (int)target_index) -> str`
- `get_modulation_value((WavetableDevice)self, (int)target_index, (int)source) -> float`
- `is_parameter_modulatable((WavetableDevice)self, (DeviceParameter)parameter) -> bool`
- `save_preset_to_compare_ab_slot((Device)arg1) -> None`
- `set_modulation_value((WavetableDevice)self, (int)target_index, (int)source, (float)value) -> None`
- `store_chosen_bank((Device)arg1, (int)arg2, (int)arg3) -> None`

Listeners (`add_/remove_/…_has_listener`): `filter_routing`, `is_active`, `is_using_compare_preset_b`, `latency_in_ms`, `latency_in_samples`, `modulation_matrix_changed`, `mono_poly`, `name`, `oscillator_1_effect_mode`, `oscillator_1_wavetable_category`, `oscillator_1_wavetable_index`, `oscillator_1_wavetables`, `oscillator_2_effect_mode`, `oscillator_2_wavetable_category`, `oscillator_2_wavetable_index`, `oscillator_2_wavetables`, `parameters`, `poly_voices`, `unison_mode`, `unison_voice_count`, `visible_modulation_target_names`

### `WavetableDevice.View`
*bases: LomObject — Representing the view aspects of a device. — seen live*

| property | writable | doc |
|---|---|---|
| `_live_ptr` | R |  |
| `canonical_parent` | R | Get the canonical parent of the View. |
| `is_collapsed` | RW | Get/Set/Listen if the device is shown collapsed in the device chain. |

Listeners (`add_/remove_/…_has_listener`): `is_collapsed`

